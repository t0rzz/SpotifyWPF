<UserControl x:Class="SpotifyWPF.View.Page.LoginPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding SpotifyLoginCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Border Background="{DynamicResource CardBrush}"
                BorderBrush="{DynamicResource ControlBorderBrush}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="24"
                Width="520"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Connect your Spotify account"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>
                <TextBlock Text="A browser window will open for authentication. Please confirm access to continue."
                           Foreground="{DynamicResource SubtleForegroundBrush}"
                           Margin="0,0,0,12"/>
                <ProgressBar IsIndeterminate="True" Height="6"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
