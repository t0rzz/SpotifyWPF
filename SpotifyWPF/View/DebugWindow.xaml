<Window x:Class="SpotifyWPF.View.DebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpotifyWPF.View"
        mc:Ignorable="d"
        Title="Debug Console" Height="600" Width="800"
        Background="{DynamicResource AppBackgroundBrush}"
        FontFamily="Consolas" FontSize="11"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="8">
                <Button Content="Clear" Command="{Binding ClearCommand}" Padding="8,4" Margin="0,0,8,0"/>
                <Button Content="Copy All" Command="{Binding CopyAllCommand}" Padding="8,4" Margin="0,0,8,0"/>
                <CheckBox Content="Auto-scroll" IsChecked="{Binding AutoScrollEnabled}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding MessageCount, StringFormat='Messages: {0}'}" Margin="16,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource ControlForegroundBrush}"/>
            </StackPanel>
        </Border>

        <!-- Debug Output -->
        <ScrollViewer Grid.Row="1" x:Name="ScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <TextBox x:Name="DebugTextBox"
                     Text="{Binding DebugText, Mode=OneWay}"
                     IsReadOnly="True"
                     Background="Transparent"
                     Foreground="{DynamicResource ControlForegroundBrush}"
                     BorderThickness="0"
                     TextWrapping="NoWrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Disabled"
                     HorizontalScrollBarVisibility="Disabled"
                     Padding="8"/>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
            <TextBlock Text="{Binding StatusText}" Margin="8" Foreground="{DynamicResource ControlForegroundBrush}" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>