<UserControl x:Class="SpotifyWPF.View.ConfirmationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400"
             MaxWidth="500" MaxHeight="300"
             Background="{DynamicResource CardBrush}"
             BorderBrush="{DynamicResource ControlBorderBrush}"
             BorderThickness="1">
    <UserControl.Resources>
        <Style x:Key="DialogButtonStyle" TargetType="Button" BasedOn="{x:Null}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ControlForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ControlHighlightBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ControlHighlightForegroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ControlPressedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsDefault" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid FocusManager.FocusedElement="{Binding ElementName=CancelButton}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="16" FontWeight="Bold"
                   Foreground="{DynamicResource ControlForegroundBrush}" Margin="16,16,16,8"
                   HorizontalAlignment="Center"/>

        <!-- Message -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="16,8">
            <TextBlock Text="{Binding Message}" FontSize="14"
                       Foreground="{DynamicResource ControlForegroundBrush}"
                       TextWrapping="Wrap" HorizontalAlignment="Center"/>
        </ScrollViewer>

        <!-- Buttons -->
        <WrapPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="16" Orientation="Horizontal">
            <Button x:Name="CancelButton" Content="{Binding CancelButtonText}" Command="{Binding CancelCommand}"
                    Style="{StaticResource DialogButtonStyle}" Visibility="{Binding CancelButtonVisibility}"
                    IsCancel="True" AutomationProperties.Name="Cancel"/>
            <Button Content="{Binding ConfirmButtonText}" Command="{Binding ConfirmCommand}"
                    Style="{StaticResource DialogButtonStyle}" IsDefault="True" AutomationProperties.Name="Confirm"/>
        </WrapPanel>
    </Grid>
</UserControl>