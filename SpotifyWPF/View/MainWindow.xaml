<Window x:Class="SpotifyWPF.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"        
        xmlns:local="clr-namespace:SpotifyWPF.View"
        xmlns:vm="clr-namespace:SpotifyWPF.ViewModel"
        xmlns:page="clr-namespace:SpotifyWPF.View.Page"
        xmlns:vm1="clr-namespace:SpotifyWPF.ViewModel.Page"
        xmlns:component="clr-namespace:SpotifyWPF.ViewModel.Component"
        mc:Ignorable="d"
        Title="Spotify WPF" Height="800" Width="1100" WindowStartupLocation="CenterScreen" WindowState="Maximized"
        Background="{DynamicResource AppBackgroundBrush}" FontFamily="Segoe UI" FontSize="13" SnapsToDevicePixels="True" UseLayoutRounding="True">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm1:LoginPageViewModel}">
            <page:LoginPage/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm1:PlaylistsPageViewModel}">
            <page:PlaylistsPage/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm1:SearchPageViewModel}">
            <page:SearchPage/>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <Menu DockPanel.Dock="Top" ItemsSource="{Binding MenuItems}">
            <Menu.ItemContainerStyle>
                <Style TargetType="{x:Type MenuItem}">
                    <Setter Property="Command" Value="{Binding Command}"/>
                    <Setter Property="CommandParameter" Value="{Binding}"/>
                    <Setter Property="IsChecked" Value="{Binding IsChecked}"/>
                    <!-- Foreground per default: chiaro (barra menu) -->
                    <Setter Property="Foreground" Value="{DynamicResource ControlForegroundBrush}"/>
                    <Style.Triggers>
                        <!-- Top level: barra menu (testo chiaro su sfondo scuro) -->
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter Property="Foreground" Value="{DynamicResource ControlForegroundBrush}"/>
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelItem">
                            <Setter Property="Foreground" Value="{DynamicResource ControlForegroundBrush}"/>
                        </Trigger>
                        <!-- Sottomenu: popup chiaro di sistema => testo scuro esplicito -->
                        <Trigger Property="Role" Value="SubmenuItem">
                            <Setter Property="Foreground" Value="#222222"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter Property="Foreground" Value="#222222"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Menu.ItemContainerStyle>

            <Menu.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type component:MenuItemViewModel}" ItemsSource="{Binding Path=MenuItems}">
                    <!-- Il Foreground del testo segue quello del MenuItem (definito sopra) -->
                    <TextBlock Text="{Binding Header}"
                               Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=MenuItem}}"/>
                </HierarchicalDataTemplate>
            </Menu.ItemTemplate>
        </Menu>
        
        <ContentControl DockPanel.Dock="Bottom" x:Name="Pages" Content="{Binding CurrentPage}"/>
    </DockPanel>
</Window>